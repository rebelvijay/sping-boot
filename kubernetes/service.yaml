apiVersion: v1
kind: Service
metadata:
  name: springboot-demo
  labels:
    app: springboot-demo

spec:
  type: NodePort

  selector:
    app: springboot-demo

  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 31000